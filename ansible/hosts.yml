# This host file is provided as an example only but it's not used by Vagrant.
# Instead check the host file that is autogenerated by Vagrant in the path:
# ../.vagrant/provisioners/ansible/inventory/vagrant_ansible_inventory

all:
  children:
    ac:
      hosts:
        10.0.1.4:
    ip:
      hosts:
        10.0.1.5:
    ag:
      hosts:
        10.0.1.6:
# ansible_user=vagrant
# ansible_ssh_pass=vagrant
# ansible_ssh_private_key= ~/.vagrant.d/insecure_private_key
